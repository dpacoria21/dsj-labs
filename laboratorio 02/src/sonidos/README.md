# Sonidos opcionales del enunciado

Coloca aquí `background.wav` (música en bucle), `laser.wav` (disparo) y `explosion.wav` (impacto).

Los sonidos se cargan al iniciar el juego. Si faltan o no hay dispositivo de audio, el juego funciona en silencio.
